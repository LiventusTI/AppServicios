
@{
    ViewBag.Title = "VisualizarPerfiles";
    Layout = "~/Views/MasterPage.cshtml";
}

<div class="row">
    <div class="col-sm-12 col-md-6 col-lg-3">
        <p><a class="btn btn-primary btn-lg" href="#" role="button" id="CrearPerfil">Crear Perfil</a></p>
    </div>
</div>
<div class="row">
    <div class="col-sm-12 col-md-12 col-lg-12">
        <table id="datatable" class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>ID Perfil</th>
                    <th>Perfil</th>
                    <th>Estado</th>
                    <th>Opciones</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>1</td>
                    <td>Administrativo antepuerto</td>
                    <td>Activo</td>
                    <td style="font-size:18px; text-align:center">
                        <i class="fa fa-cog " id="Editar" title="Editar Perfil"></i>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

<script>
    //Botón Crear Perfil//
    $("#CrearPerfil").click(function () {
        swal({
            showCancelButton: true,
            cancelButtonText: "Cancelar",
            title: 'Crear Perfil',
            html:
                '<div class="row">'+
                    '<div class="form-group col-lg-6"><label for="Perfil">Perfil</label><input type="text" class="form-control" id="Perfil" placeholder="Escriba Perfil"></div>' +
                    '<div class="form-group col-lg-6"><label for="Estado">Estado</label><select class="form-control" id="Estado"><option value="0">Activo</option><option value="1">Deshabilitado</option></select></div>' +
                '</div>',
            preConfirm: function () {
                return new Promise(function (resolve) {
                    resolve([
                        //asignar valores a las variables
                    ])
                })
            },
            onOpen: function () {
                $('#Perfil').focus()
            }
        }).then(function (result) {
            //enviar datos para agregar Perfil
        }).catch(swal.noop)
    });
    //Botón Crear Perfil//
    //Botón Editar Perfil//
    $("#Editar").click(function () {
        var valores = [];
        var i = 0;
        // Obtenemos todos los valores contenidos en los <td> de la fila
        // seleccionada
        $(this).parents("tr").find("td").each(function () {
            valores[i++] = $(this).html();
        });
        //obtener datos del perfil seleccionado
        swal({
            showCancelButton: true,
            cancelButtonText: "Cancelar",
            title: 'Editar Perfil',
            html:
                '<div class="row">' +
                    '<div class="form-group col-lg-6"><label for="Perfil">Perfil</label><input type="text" class="form-control" id="Perfil" placeholder="Escriba Perfil"></div>' +
                    '<div class="form-group col-lg-6"><label for="Estado">Estado</label><select class="form-control" id="Estado"><option value="0">Activo</option><option value="1">Deshabilitado</option></select></div>' +
                '</div>',
            preConfirm: function () {
                return new Promise(function (resolve) {
                    resolve([
                        //Obtener nuevos Datos Ingresados
                    ])
                })
            },
            onOpen: function () {
                $('#Perfil').focus()
            }
        }).then(function (result) {

        }).catch(swal.noop)
    });
    //Botón Editar Perfil//

</script>