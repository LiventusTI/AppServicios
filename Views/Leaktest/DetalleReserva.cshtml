
@{
    ViewBag.Title = "DetalleReserva";
    Layout = "~/Views/MasterPage.cshtml";
}

<div class="row">
    <div class="col-sm-12 col-md-12 col-lg-12">
        <table id="datatable" class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>ID Leaktest</th>
                    <th>Fecha</th>
                    <th>Service Provider</th>
                    <th>Técnico</th>
                    <th>N°Leaktest Report</th>
                    <th>Duración</th>
                    <th>Contenedor</th>
                    <th colspan="1">Opciones</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>1</td>
                    <td>22/10/2017</td>
                    <td>Chile</td>
                    <td>Randal Contreras</td>
                    <td>1123asd</td>
                    <td>10 minutos</td>
                    <td>123456</td>
                    <td style="font-size:18px; text-align:center"><i class="fa fa-archive" title="Agregar Notas" id="Notas"></i></td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
<script>
    //Botón Agregar Nota//
    $("#Notas").click(function () {
        var valores = [];
        var i = 0;
        // Obtenemos todos los valores contenidos en los <td> de la fila
        // seleccionada
        $(this).parents("tr").find("td").each(function () {
            valores[i++] = $(this).html();
        });
        swal({
            showCancelButton: true,
            cancelButtonText: "Cancelar",
            title: 'Agrerar Nota',
            html:
                '<div class="row">' +
                    '<div class="form-group col-lg-12"><label for="Descripcion"></label><textarea class="form-control" id="Notas" rows="5" resize="none" placeholder="Escriba Nota Para la Reserva"></textarea></div>' +
                '</div>',
            preConfirm: function () {
                return new Promise(function (resolve) {
                    resolve([
                        //Obtener nuevos Datos Ingresados
                    ])
                })
            },
            onOpen: function () {
                //Cargar Paises
                //Cargar Ciudades Respecto a Pais Seleccionado
                $('#NombreDeposito').focus()
            }
        }).then(function (result) {

        }).catch(swal.noop)
    });
    //Botón Agregar Nota//
</script>